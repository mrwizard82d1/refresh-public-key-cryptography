[project]
name = "refresh-public-key-cryptography"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "cryptography>=46.0.2",
    "fastapi[standard]>=0.118.0",
    "ipython>=9.6.0",
    "pytest>=8.4.2",
]
